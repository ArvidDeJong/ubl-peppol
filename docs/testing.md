# Testing

Run the test suite:

```bash
./vendor/bin/pest
```

Run a specific test file:

```bash
./vendor/bin/pest tests/Feature/UblBeBis3IntegrationTest.php
```

For validation-focused changes, also verify generated XML against the official online validators listed in [Validation](validation.md).
